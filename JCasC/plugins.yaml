####################################################
# Plugins could be found at Jenkins Update Center using their IDs. e.g.
# https://plugins.jenkins.io/workflow-aggregator
####################################################

plugins:
  required:
    workflow-aggregator: 2.6
    blueocean: 1.10.2
  sites:
  - id: "default"
    url: "https://updates.jenkins.io/update-center.json"
